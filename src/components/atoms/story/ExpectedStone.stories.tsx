import React from 'react';
import ExpectedStone from '../ExpectedStone';

const title = {
  title: 'ExpectedStone',
};

export const showExpectedStone = () => <ExpectedStone />;

export default title;

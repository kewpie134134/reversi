import React from 'react';
import ReversiBoard from '../ReversiBoard';

const title = {
  title: 'ReversiBoard',
};

export const reversiBoard = (): JSX.Element => <ReversiBoard />;

export default title;

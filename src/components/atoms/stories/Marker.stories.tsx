import React from 'react';
import Marker from '../Marker';

const title = {
  title: 'Marker',
};

export const WhiteMarker = (): JSX.Element => <Marker name="whiteMarker" />;

export const BlackMarker = (): JSX.Element => <Marker name="blackMarker" />;

export default title;

import React from 'react';
import Piece from '../Piece';

const title = {
  title: 'Piece',
};

export const WhitePiece = () => <Piece color={'white'} />;

export const BlackPiece = () => <Piece color={'black'} />;

export default title;
